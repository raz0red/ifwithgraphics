# ZTools

Builder-side Z-machine inspection tooling.

Source package:

- `ztools731a.zip`
- IF Archive path: `if-archive/infocom/tools/ztools/ztools731a.zip`
- Description: ZTools 7/3.1 source updated for modern C compilers.

The source is vendored under `source/` so we can compile `infodump` with Emscripten for the browser-based builder.
